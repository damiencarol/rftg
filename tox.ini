# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py3
isolated_build = True

[testenv]
deps = pytest-xdist
#changedir = tests
# use three sub processes
commands = pytest -v -n 3 {posargs} tests/
